name: job_alert_mobile
description: Job Alert mobile controller — cloud status and job browser.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  url_launcher: ^6.3.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
